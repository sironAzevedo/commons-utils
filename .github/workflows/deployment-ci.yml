name: commons-utils

on:
  push:
    branches:
      - '*'
      - '!main'

jobs:
    build:
      uses: sironAzevedo/ci-workflows/.github/workflows/feature.yml@v0.0.18
      with:
        java-version: '17'
        GITHUB_TOKEN: secrets.GH_TOKEN